#ifndef BASE_HH
#define BASE_HH

class Base
{
	public:
		virtual ~Base( void ) throw();
/*	private:
		Base( void );
		Base( const Base & other );
		Base & operator=( const Base & other );
*/
};

#endif
